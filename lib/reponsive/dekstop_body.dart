import 'package:flutter/material.dart';

class MyDekstopBody extends StatelessWidget {
  const MyDekstopBody({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.green[300]);
  }
}